// Liason between model and view js. Conditional statments, loops, other constructs that execute different decisions based on user input.

console.log("control is linked");

if(0===0){
	var TestOneTwoThree = TestOneTwo
	console.log(TestOneTwoThree);
};
